spring:
  datasource:
    url: jdbc:postgresql://postgres-catalogue-service:5432/service1db
    username: service1user
    password: service1pass
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true